version: 2
jobs:
  one:
    working_directory: "~"
    docker:
      - image: "debian:jessie"
    steps:
      - run: echo "A first hello"
      - run: sleep 5
  two:
    working_directory: "~"
    docker:
      - image: "debian:jessie"
    steps:
      - run: echo "And a more familiar hi"
      - run: sleep 10
workflows:
  version: 2
  one_and_two:
    jobs:
      - one
      - two
